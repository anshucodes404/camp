spring:
  application:
    name: camp

  security:
    user:
      name: anshu
      password: anshu

  datasource:
    url: jdbc:postgresql://localhost:5432/camp
    username: codes404
    password: codes404

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

jwt:
  secret: iherfh3hu343948y834yfFF_#$F#F"#e2h3283772ygd273GF$G$%4
  access-token-expiry: 900000
  refresh-token-expiry: 604800000

